---
phase: 01-natywna-nawigacja
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: []
autonomous: false
requirements: [NAV-01, NAV-02]

must_haves:
  truths:
    - "After deploying 1.1.0 to the GLPI 11.0.4 host and running plugin:install + plugin:activate, a Self-Service user sees the 'Podgląd SIM' tile on /Helpdesk and clicking it opens the SIM catalog (SC1 / NAV-01)."
    - "The tile appeared without any manual tile configuration; re-running plugin:install produced no duplicate tile (SC2 / NAV-01)."
    - "Viewing page source / the network tab on a self-service page shows no public/js/nav-inject.js request and no simviewer:* meta tags (SC3 / NAV-02)."
    - "The v1.0 'Podgląd SIM' menu entry in the simplified interface still works (SC4 — no regression)."
    - "The top-nav-anchor option no longer appears on the plugin config form (SC3 / NAV-02)."
    - statement: "Clicking the tile lands on the existing catalog page with no browser console or network errors (UI backstop, target-unreachable)."
      verification: backstop
  prohibitions:
    - "The tile must not be visible to a user without the plugin READ right or on the central interface (verify with a non-entitled account)."
    - "No PIN/PUK/PIN2/PUK2/MSIN value appears anywhere on the tile or catalog page."
  artifacts:
    - "Human-verify checkpoint record confirming SC1–SC4 on the GLPI 11.0.4 production/staging host"
  key_links:
    - "Deploy path: git push → git pull on host → plugin:install + plugin:activate in the Docker console (docs/deploy-z-laptopa.md); version bump to 1.1.0 forces the update path."
---

<objective>
Verify Phase 1 on the real GLPI 11.0.4 host. Plan 01 is source-only; the phase's success criteria (SC1–SC4) are browser/behavioral and can only be confirmed after deploy on `glpi.mgw1943.local`, which Claude cannot reach. This plan is the blocking human-verify checkpoint.

Purpose: Prove the native tile appears and works, the JS injection is gone, and the v1.0 menu entry did not regress — before marking NAV-01 / NAV-02 done.
Output: A verification decision (approved or defect list) captured in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-natywna-nawigacja/01-CONTEXT.md
@.planning/phases/01-natywna-nawigacja/01-UI-SPEC.md
@.planning/phases/01-natywna-nawigacja/01-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Deploy 1.1.0 and verify native tile + JS-injection removal on GLPI 11.0.4</name>
  <action>Blocking human-verify checkpoint. Deploy the branch to the GLPI host and run plugin:install + plugin:activate, then perform the browser verification steps in <how-to-verify> below and record the outcome. No source changes are made by this task.</action>
  <what-built>
    Plan 01 registered a native GLPI 11 "Podgląd SIM" ExternalPageTile at plugin install (per helpdesk profile, idempotent, removed at uninstall), removed all custom JS-injection navigation (public/js/nav-inject.js, ADD_JAVASCRIPT/ADD_HEADER_TAG hooks, the top-nav-anchor config option and constant), and bumped the plugin to 1.1.0.
  </what-built>
  <how-to-verify>
    Deploy first (per docs/deploy-z-laptopa.md): push the branch, `git pull` on the GLPI host, then in the Docker console run the plugin CLI `plugin:install` followed by `plugin:activate` for simviewer (this exercises the update path because the version is now 1.1.0).

    Then verify on glpi.mgw1943.local:
    1. Log in as a Self-Service (helpdesk-interface) user who holds the SIM Viewer READ right. On /Helpdesk, confirm a "Podgląd SIM" tile is present.
    2. Click the tile → it must open the SIM catalog (front/simcard.php) showing the read-only directory. Check the browser console/network tab: no errors, HTTP 200.
    3. View page source / the network tab on a self-service page: confirm there is NO request for public/js/nav-inject.js and NO <meta name="simviewer:*"> tags.
    4. Confirm the v1.0 "Podgląd SIM" entry in the simplified-interface menu still works (open it → same catalog).
    5. In the central interface, open the plugin config form (Setup → General → SIM Viewer tab): confirm the "Top nav-bar anchor (CSS selector)" field is gone and every other field is intact.
    6. Idempotency: re-run `plugin:install` and reload /Helpdesk → still exactly one "Podgląd SIM" tile (no duplicate).
    7. Negative check: log in as a user WITHOUT the SIM Viewer READ right (or on the central interface) → the tile must NOT appear.
    8. Optional but recommended: run `plugin:uninstall` on a staging copy → confirm the tile disappears from /Helpdesk; then reinstall.
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 checks pass, or list which step failed (with what you saw) so the implementation plan can be revised.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| helpdesk user → /Helpdesk tile | Verification confirms the tile is scoped to right-holders on the simplified interface only |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Information Disclosure | tile visible to non-entitled user | medium | mitigate | Verified at checkpoint step 7 (negative account) and step 1 (entitled account) — confirms the profile-scoped registration from Plan 01 actually hides the tile from non-right-holders and central-interface users. |
| T-01-SC | Tampering (supply chain) | package installs | n/a | accept | No package installs; verification only. |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)
- No source artifacts. Produces a verification decision (approved / defect list) recorded in `01-02-SUMMARY.md`, confirming SC1–SC4 on the live GLPI 11.0.4 host.
</artifacts_produced>

<verification>
Human-verify checkpoint only — behavioral confirmation of SC1–SC4 in a real browser after deploy. No automated gate (Claude cannot reach the host).
</verification>

<success_criteria>
- All checkpoint steps pass on the GLPI 11.0.4 host, or defects are captured for revision.
- NAV-01 and NAV-02 confirmed behaviorally.
</success_criteria>

<output>
Create `.planning/phases/01-natywna-nawigacja/01-02-SUMMARY.md` when done. Record the verification outcome (approved/defects), the deploy notes (install vs. update path, any `.mo` regeneration performed on the host), and the actual tile illustration rendered.
</output>
