---
phase: 02-eksport-csv-i-filtr-pustych-sim-w
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified: []
autonomous: false
requirements: [EXP-01, EXP-02, TBL-01]
user_setup:
  - service: production GLPI (glpi.mgw1943.local, GLPI 11.0.4, Docker)
    why: "PHP nie działa lokalnie; kryteria SC1-SC4 (42/136 wierszy, widoczność przycisku, 403) są weryfikowalne wyłącznie w przeglądarce/HTTP na produkcji po deployu 1.2.0"
    dashboard_config:
      - task: "git push (lokalnie) -> git pull na serwerze -> w konsoli dockera: plugin:install simviewer + plugin:activate simviewer"
        location: "serwer glpi.mgw1943.local, kontener GLPI"
must_haves:
  truths:
    - "Po deployu 1.2.0 katalog SIM na produkcji domyślnie pokazuje 42 wiersze (nie 136) (SC3)"
    - "Włączenie show_unassigned=1 w konfiguracji pokazuje 136 wierszy; wyłączenie wraca do 42 (SC4)"
    - "enable_export jest WŁĄCZONY po aktualizacji z 1.1.0 (produkcja miała '0') — przycisk Export widoczny na datatable (SC2/SC1)"
    - "Pobrany CSV ma 42 wiersze danych (przy domyślnym filtrze), kolumny User/Phone (+serial/status wg configu), separator ';', otwiera się poprawnie w Excel PL; brak PIN/PUK/PIN2/PUK2/MSIN (SC1)"
    - "Wyłączenie enable_export ukrywa przycisk Export ORAZ bezpośredni GET front/export.php zwraca błąd uprawnień 403 bez danych (SC2)"
  artifacts:
    - ".planning/phases/02-eksport-csv-i-filtr-pustych-sim-w/02-03-SUMMARY.md z wynikiem UAT SC1-SC4"
  key_links:
    - "Deploy 1.2.0 wyzwala jednorazową migrację flip enable_export (Plan 01) — weryfikowalną jako 'przycisk pojawił się bez ręcznej zmiany configu'"
---

<objective>
Wdróż wersję 1.2.0 na produkcję (glpi.mgw1943.local, GLPI 11.0.4) i zweryfikuj w przeglądarce/HTTP kryteria sukcesu Phase 2 (SC1-SC4): natywny eksport CSV, serwerowe sterowanie enable_export (przycisk + 403), domyślne ukrycie pustych SIM-ów (42 vs 136) i przełącznik show_unassigned. PHP nie jest uruchamialny lokalnie, więc te kryteria behawioralne są weryfikowalne wyłącznie tutaj.

Purpose: Domknięcie Phase 2 dowodem na żywej produkcji.
Output: 02-03-SUMMARY.md z wynikiem UAT.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-eksport-csv-i-filtr-pustych-sim-w/02-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Deploy 1.2.0 and verify CSV export + empty-SIM filter on GLPI 11.0.4 (SC1-SC4)</name>
  <action>Blocking human-verify checkpoint. Deploy the branch to the production GLPI host and run plugin:install (update path 1.1.0->1.2.0) + plugin:activate, then perform the browser/HTTP verification steps in <how-to-verify> and record the per-SC outcome in the SUMMARY. No source changes are made by this task.</action>
  <what-built>
    Wersja 1.2.0 wtyczki: natywny eksport CSV (front/export.php + csv_url na datatable), serwerowe sterowanie enable_export (domyślnie WŁĄCZONE po aktualizacji, 403 gdy wyłączone), domyślny filtr ukrywający SIM-y bez przypisanego użytkownika (show_unassigned, default 0), bump do 1.2.0. Wszystkie zmiany są scommitowane; PHP nie był uruchamiany lokalnie — weryfikacja tylko statyczna (grep/lint).
  </what-built>
  <how-to-verify>
    Deploy (produkcja glpi.mgw1943.local, GLPI 11.0.4):
    1. Lokalnie: `git push`. Na serwerze: `git pull` w katalogu wtyczki.
    2. W konsoli kontenera GLPI: `php bin/console plugin:install simviewer` (ścieżka update — podbija 1.1.0 -> 1.2.0), następnie `php bin/console plugin:activate simviewer`. Zanotuj brak błędów migracji.

    Weryfikacja SC3 (domyślne ukrycie pustych SIM-ów):
    3. Zaloguj się profilem self-service, otwórz katalog „Podgląd SIM". Potwierdź: tabela pokazuje 42 wiersze (nie 136). Wszystkie mają wypełnioną kolumnę User.

    Weryfikacja SC4 (przełącznik show_unassigned):
    4. Jako admin (config UPDATE): Ustawienia -> konfiguracja wtyczki SIM Viewer -> ustaw „Show SIMs without assigned user" = Yes, zapisz. Odśwież katalog: 136 wierszy. Wróć do No: znowu 42.

    Weryfikacja SC1/SC2 (eksport + sterowanie enable_export):
    5. Z enable_export = Yes (spodziewane domyślnie po aktualizacji — potwierdza jednorazową migrację flip; produkcja miała zapisane '0'): na datatable widoczny natywny przycisk Export (ikona download). Kliknij — pobiera `sim-viewer_RRRR-MM-DD.csv`.
    6. Otwórz CSV w Excel PL: separator ';' rozpoznany, polskie znaki poprawne (BOM), 42 wiersze danych przy domyślnym widoku, kolumny = User/Phone (+serial/status jeśli włączone). Potwierdź BRAK jakiejkolwiek kolumny PIN/PUK/PIN2/PUK2/MSIN.
    7. Wpisz filtr tekstowy na stronie, ponownie eksportuj — CSV zawiera tylko przefiltrowane wiersze (ten sam widok).
    8. W konfiguracji ustaw „Enable CSV export" = No, zapisz. Odśwież katalog: przycisk Export znika. Wejdź bezpośrednio na URL `.../plugins/simviewer/front/export.php` — spodziewany błąd uprawnień (403), ZERO danych CSV. Przywróć enable_export = Yes.

    Weryfikacja braku regresji:
    9. Potwierdź, że kafelek „Podgląd SIM" na /Helpdesk i wpis w menu uproszczonym nadal działają (Phase 1 — brak regresji po bumpie wersji).

    OPCJONALNIE (nie jest bramką Phase 2) — domknięcie deferred z Phase 1:
    10. Deploy 1.2.0 to okazja do wykonania odłożonego testu deinstalacji z 01-UAT.md: `php bin/console plugin:uninstall simviewer` usuwa kafelek „Podgląd SIM" z /Helpdesk; ponowny install przywraca dokładnie jeden kafelek. Jeśli wykonasz — odnotuj wynik i uruchom `/gsd-verify-work 1` by domknąć Deferred Verification. To ekstra, nie warunek zaliczenia Phase 2.
  </how-to-verify>
  <resume-signal>Napisz „approved" jeśli SC1-SC4 przeszły, albo opisz rozbieżności (np. liczba wierszy, przycisk, zachowanie 403).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy pipeline -> production | git pull + plugin:install on live GLPI; migration mutates stored config (enable_export flip) |
| direct URL -> front/export.php | Human tester verifies the 403 gate by hitting the endpoint directly with export disabled |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-10 | Information disclosure | production CSV export | high | mitigate | Human verification step 6 explicitly confirms no PIN/PUK/PIN2/PUK2/MSIN column in the downloaded file |
| T-02-11 | Elevation of privilege | export endpoint with export disabled | high | mitigate | Human verification step 8 confirms direct-URL GET returns 403 with no data when enable_export=0 |
| T-02-12 | Tampering | one-time migration on live data | high | mitigate | Step 5 confirms flip happened once (button visible) without manual config change; marker prevents re-flip on future installs |
| T-02-SC | Tampering | dependency installs | low | accept | No third-party packages added this phase |
</threat_model>

<verification>
Blocking human checkpoint on production. SC1-SC4 confirmed by browser/HTTP UAT; no automated substitute exists locally (PHP not runnable). Record pass/fail per SC in the SUMMARY.
</verification>

<success_criteria>
- Deploy 1.2.0 succeeds (no migration errors)
- SC1 (native export of current view, no sensitive fields), SC2 (button hidden + 403 when disabled; enabled by default after update), SC3 (42 rows default), SC4 (show_unassigned restores 136) all human-verified
</success_criteria>

<output>
Create `.planning/phases/02-eksport-csv-i-filtr-pustych-sim-w/02-03-SUMMARY.md` with the per-SC UAT result and the deploy notes. If the optional Phase 1 uninstall test was run, note its result and point to `/gsd-verify-work 1`.

## Artifacts this phase produces (Plan 03)
- `.planning/phases/02-eksport-csv-i-filtr-pustych-sim-w/02-03-SUMMARY.md`: production UAT record for SC1-SC4 + deploy log (1.1.0 -> 1.2.0).
</output>
