---
phase: 02-eksport-csv-i-filtr-pustych-sim-w
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Config.php
  - src/Simcard.php
  - hook.php
  - setup.php
  - locales/pl_PL.po
  - locales/en_GB.po
  - locales/pl_PL.mo
  - locales/en_GB.mo
autonomous: true
requirements: [TBL-01, EXP-02]
must_haves:
  truths:
    - "Z domyślnym show_unassigned=0, na produkcji katalog pokazuje 42 wiersze (SIM-y z przypisanym użytkownikiem), nie 136 (SC3)"
    - "Ustawienie show_unassigned=1 przywraca pełną listę 136 wierszy na produkcji (SC4)"
    - "Po aktualizacji wtyczki z 1.1.0 do 1.2.0 opcja enable_export jest WŁĄCZONA (wartość '1'), mimo że produkcja miała zapisane '0' (SC2, D: EXP-02)"
    - "Formularz konfiguracji SIM Viewer zawiera nowe pole Yes/No 'Show SIMs without assigned user' (show_unassigned)"
    - "PLUGIN_SIMVIEWER_VERSION wynosi 1.2.0 — wymusza ścieżkę update na produkcji"
  artifacts:
    - "src/Config.php: klucz konfiguracji show_unassigned (default '0'), enable_export default '1'"
    - "src/Simcard.php: warunek WHERE users_id > 0 w getRows() gdy show_unassigned falsy"
    - "hook.php: jednorazowa migracja flip enable_export '0'->'1' z markerem export_default_migrated"
    - "setup.php: define PLUGIN_SIMVIEWER_VERSION '1.2.0'"
    - "locales/*.po + *.mo: string 'Show SIMs without assigned user'"
  key_links:
    - "getRows() czyta Config::getConfig()['show_unassigned'] wewnętrznie — show() i front/export.php (Plan 02) automatycznie dziedziczą filtr bez przekazywania parametru"
    - "Migracja flip enable_export gated markerem export_default_migrated — nie nadpisuje późniejszej decyzji admina (SC2 + prohibition)"
  prohibitions:
    # Flagged-unverified value/safety guards. No check_* descriptor — human/checkpoint judgement.
    - "[UNVERIFIED] Filtr show_unassigned musi działać w SQL WHERE (getRows), nigdy w warstwie widoku — spójnie z entity scopingiem"
    - "[UNVERIFIED] Migracja flipu enable_export musi zadziałać dokładnie raz i nigdy nie nadpisać decyzji admina o wyłączeniu podjętej po wejściu na 1.2.0"
    - "[UNVERIFIED] Definicja 'pustego' SIM-a jest po users_id (0/NULL), nie po sformatowanej nazwie użytkownika"
  edge_probes:
    # Spec-less deterministic probe rows — surfaced, never silently dropped.
    - "[TBL-01 / unclassified — review manually] Zachowanie przy braku dopasowań filtra + show_unassigned=0: getRows zwraca pustą listę, widok pokazuje alert 'No SIM cards are visible to you.' (istniejąca ścieżka)"
    - "[EXP-02 / concurrency] Migracja flipu enable_export biegnie w plugin_simviewer_install() (jednowątkowy proces konsoli plugin:install); marker export_default_migrated czyni ją idempotentną przy powtórnym uruchomieniu"
---

<objective>
Domyślnie ukryj SIM-y bez przypisanego użytkownika (TBL-01) i przygotuj sterowanie eksportem (EXP-02): flip domyślnego `enable_export` na WŁĄCZONE z jednorazową bezpieczną migracją, nowa opcja `show_unassigned`, bump wersji do 1.2.0. Endpoint eksportu i przycisk `csv_url` powstają w Planie 02.

Purpose: Katalog na produkcji ma pokazywać 42 istotne wiersze zamiast 136 (94 puste SIM-y znikają), a eksport musi być domyślnie włączony po aktualizacji — bez nadpisywania przyszłych decyzji admina.
Output: Zmodyfikowane src/Config.php, src/Simcard.php (getRows), hook.php (migracja), setup.php (1.2.0), locales pl/en (+.mo).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-eksport-csv-i-filtr-pustych-sim-w/02-CONTEXT.md
@.planning/phases/02-eksport-csv-i-filtr-pustych-sim-w/02-PATTERNS.md
</context>

<assumption_delta_decision>
TBL-01 zmienia domyślną populację katalogu z "wszystkie SIM-y w encji" na "tylko przypisane" (required->default-scope transition). EXP-02 odwraca privacy-first default eksportu.
Decision: **promote** (obie zmiany domyślnych są świadomymi decyzjami użytkownika z 2026-07-20, udokumentowanymi w PROJECT.md Key Decisions i ROADMAP SC2/SC3). Nowa opcja `show_unassigned` dodana **alongside** — default '0' zachowuje nowe, węższe zachowanie, ale admin może przywrócić stare (136 wierszy) jednym przełącznikiem. Brak konieczności eskalacji: obie zmiany mają jawne pokrycie w źródłach.
</assumption_delta_decision>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Config — show_unassigned key, enable_export default flip, normalize, form field</name>
  <files>src/Config.php, locales/pl_PL.po, locales/en_GB.po, locales/pl_PL.mo, locales/en_GB.mo</files>
  <read_first>
    - src/Config.php (getDefaults :63-80, configUpdate :183-191, showConfigForm :193-252) — the file being modified
    - .planning/phases/02-eksport-csv-i-filtr-pustych-sim-w/02-PATTERNS.md (sections "src/Config.php" and "Idempotent config seeding")
    - locales/pl_PL.po and locales/en_GB.po (existing "Enable CSV export" msgid block — the format to mirror)
    - tools/po2mo.py (the .mo regeneration tool)
  </read_first>
  <behavior>
    - getDefaults() returns 'enable_export' => '1' (flipped from '0')
    - getDefaults() returns a new 'show_unassigned' => '0' key
    - configUpdate() normalizes 'show_unassigned' to '0'/'1' string alongside existing checkboxes
    - showConfigForm() renders a Yes/No dropdown for show_unassigned with label "Show SIMs without assigned user"
    - Config::getConfig() on an install with no stored show_unassigned returns '0' (default merge)
  </behavior>
  <action>
    In getDefaults() (src/Config.php:63-80): change `'enable_export' => '0'` to `'enable_export' => '1'` (D: EXP-02), and add a new array entry `'show_unassigned' => '0'` (D: TBL-01). Do NOT add any migration-marker key to getDefaults — the marker lives only in stored config (Task 2 of this plan, hook.php).
    In configUpdate() (:186): add `'show_unassigned'` to the `foreach (['show_serial', 'show_status', 'enable_export'] as $bool)` array so the checkbox normalizes to '0'/'1'.
    In showConfigForm(): immediately after the existing "Enable CSV export" row (:219-221), add an identical `tab_bg_1` table row using `__s('Show SIMs without assigned user', 'simviewer')` as the label and `Dropdown::showYesNo('show_unassigned', $cfg['show_unassigned'], -1, ['readonly' => !$can_edit]);` as the control. Keep the exact same markup idiom (td/td, htmlspecialchars not needed for the dropdown).
    Locales: append a `msgid "Show SIMs without assigned user"` / `msgstr` pair to BOTH locales/pl_PL.po (Polish: "Pokaż SIM-y bez przypisanego użytkownika") and locales/en_GB.po (English echo). Then regenerate both .mo binaries by running the po2mo tool (do not hand-edit .mo). Do not touch the existing "Enable CSV export" translations.
    Rationale note in code comment: enable_export default flip is the 2026-07-20 user decision that consciously relaxes the PRD RODO recommendation.
  </action>
  <verify>
    <automated>grep -q "'show_unassigned' => '0'" src/Config.php && grep -q "'enable_export' => '1'" src/Config.php && grep -q "Show SIMs without assigned user" src/Config.php && grep -q "showYesNo('show_unassigned'" src/Config.php && grep -q "Show SIMs without assigned user" locales/en_GB.po && grep -q "Show SIMs without assigned user" locales/pl_PL.po && python tools/po2mo.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'enable_export' => '0'" src/Config.php` returns 0 (old default gone)
    - `grep -c "'enable_export' => '1'" src/Config.php` returns 1
    - `grep -c "'show_unassigned' => '0'" src/Config.php` returns 1 (in getDefaults)
    - configUpdate foreach array line contains the token show_unassigned (verify: `grep -n "show_status', 'enable_export', 'show_unassigned'" src/Config.php` OR the token present in the normalize foreach)
    - showConfigForm contains `showYesNo('show_unassigned'`
    - Both .po files contain msgid "Show SIMs without assigned user"; po2mo.py exits 0 and regenerates both .mo files
    - If PHP available locally: `php -l src/Config.php` reports no syntax errors
  </acceptance_criteria>
  <done>enable_export default is '1', show_unassigned key exists (default '0'), normalized on save, rendered as Yes/No in the config form, and translated in pl/en (.po + regenerated .mo).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: getRows() — hide unassigned SIMs via SQL WHERE users_id > 0</name>
  <files>src/Simcard.php</files>
  <read_first>
    - src/Simcard.php (getRows :98-202, especially the WHERE block :146-151 and `$cfg = Config::getConfig();` :103)
    - .planning/phases/02-eksport-csv-i-filtr-pustych-sim-w/02-PATTERNS.md (section "src/Simcard.php — getRows() WHERE filter")
  </read_first>
  <behavior>
    - When Config show_unassigned is '0' (falsy), getRows() SQL WHERE includes a condition on glpi_items_devicesimcards.users_id > 0
    - When show_unassigned is '1' (truthy), no users_id restriction is added — full list returned
    - The filter is on the base table users_id column (catches 0 and NULL joins), never on formatted user name
    - No new SELECT column is added; no signature change to getRows(?string $filter = null)
  </behavior>
  <action>
    In getRows() (src/Simcard.php), extend the existing WHERE-building block at :146-151. `$cfg` is already fetched at :103 (`$cfg = Config::getConfig();`). After pushing the entity_restrict criteria, add a conditional: when `empty($cfg['show_unassigned'])`, push a restriction on the base table column onto `$where` using the GLPI DBmysql criteria form `"{$isc}.users_id" => ['>', 0]` (D: TBL-01). Filter MUST target `{$isc}.users_id` (the glpi_items_devicesimcards column), not the joined glpi_users fields, so NULL joins are excluded too. Do NOT thread a new parameter through the signature — getRows() reads Config internally, so both show() and the future front/export.php (Plan 02) share this filter automatically. Add a short comment mirroring the existing "Entity scoping is enforced in SQL" note: the unassigned filter is enforced in SQL, never in the view layer.
  </action>
  <verify>
    <automated>grep -Eq "users_id\"?\s*=>\s*\['>',\s*0\]" src/Simcard.php && grep -q "show_unassigned" src/Simcard.php</automated>
  </verify>
  <acceptance_criteria>
    - src/Simcard.php getRows() references `$cfg['show_unassigned']` and adds `"{$isc}.users_id" => ['>', 0]` (or equivalent `['>', 0]` criteria) to `$where` only when show_unassigned is empty
    - The condition sits inside getRows() WHERE construction (before the `$DB->request`), verified by `grep -n "users_id.*'>'" src/Simcard.php` line being between the entity_restrict block and the `$DB->request([` call
    - getRows signature is unchanged: `grep -c "public static function getRows(?string \$filter = null)" src/Simcard.php` returns 1
    - No sensitive column added to `$select`: `grep -Eic 'pin|puk|msin' src/Simcard.php` returns 0
    - If PHP available locally: `php -l src/Simcard.php` reports no syntax errors
  </acceptance_criteria>
  <done>getRows() returns only assigned SIMs by default (users_id > 0), full list when show_unassigned=1, filter enforced in SQL on the base table column, shared by show() and the export endpoint.</done>
</task>

<task type="auto">
  <name>Task 3: hook.php one-time enable_export flip migration + setup.php bump to 1.2.0</name>
  <files>hook.php, setup.php</files>
  <read_first>
    - hook.php (plugin_simviewer_install :67-150, especially Config::install() call :94 and the nav_selector deletion idiom :96-102)
    - setup.php (PLUGIN_SIMVIEWER_VERSION define :39)
    - src/Config.php (CONTEXT constant :54, install() :94-103)
    - .planning/phases/02-eksport-csv-i-filtr-pustych-sim-w/02-PATTERNS.md (section "hook.php — one-time enable_export migration")
  </read_first>
  <action>
    In setup.php:39 change `define('PLUGIN_SIMVIEWER_VERSION', '1.1.0');` to `'1.2.0'`.
    In hook.php plugin_simviewer_install(): implement a one-time, admin-decision-safe flip of stored enable_export (D: EXP-02, satisfies SC2 without clobbering later admin choice — the prohibition). Concrete logic:
      1. BEFORE the existing `Config::install();` call at :94, capture the pre-install stored config: `$pre_existing = \Config::getConfigurationValues(Config::CONTEXT);` (empty array on a truly fresh install; populated on upgrade). Determine `$is_upgrade = !empty($pre_existing);`.
      2. After Config::install() (and near the nav_selector cleanup), add the flip block gated on a one-time marker key `export_default_migrated`: if `!isset($pre_existing['export_default_migrated'])`, then (a) only when `$is_upgrade` AND `($pre_existing['enable_export'] ?? null) === '0'`, write `(new \Config())->setConfigurationValues(Config::CONTEXT, ['enable_export' => '1']);`; (b) always set the marker `(new \Config())->setConfigurationValues(Config::CONTEXT, ['export_default_migrated' => '1']);`.
    This guarantees: fresh 1.2.0 install seeds enable_export='1' via defaults and sets the marker WITHOUT a flip (so a later admin disable survives future reinstalls); upgrade from 1.1.0 with stored '0' (production) flips to '1' exactly once; any subsequent install run sees the marker and does nothing. Mirror the existing `(new \Config())->...` instantiation style used by the nav_selector deletion.
    Do NOT rely on comparing PLUGIN_SIMVIEWER_VERSION strings for the guard — the marker is the source of truth for "already migrated" and is race-free within the single-threaded console install process.
  </action>
  <verify>
    <automated>grep -q "PLUGIN_SIMVIEWER_VERSION', '1.2.0'" setup.php && grep -q "export_default_migrated" hook.php && grep -q "'enable_export' => '1'" hook.php && grep -q "\$pre_existing" hook.php</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PLUGIN_SIMVIEWER_VERSION', '1.1.0'" setup.php` returns 0; `'1.2.0'` returns 1
    - hook.php reads pre-install config before Config::install(): a `\Config::getConfigurationValues(Config::CONTEXT)` read assigned to a variable appears at a line number LOWER than the `Config::install();` call
    - hook.php sets marker `export_default_migrated` and conditionally sets `enable_export` => '1' guarded by `!isset($pre_existing['export_default_migrated'])` and `$is_upgrade`
    - The flip is NOT unconditional: `grep -n "enable_export' => '1'" hook.php` line is inside an `if` gated on both the marker-absent check and the `=== '0'` pre-existing value check
    - If PHP available locally: `php -l hook.php` and `php -l setup.php` report no syntax errors
  </acceptance_criteria>
  <done>Version is 1.2.0; upgrading a production install (stored enable_export '0') flips it to '1' exactly once; fresh installs and post-migration admin disables are never clobbered (marker-gated).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin config form -> glpi_configs | Config UPDATE-holders submit enable_export / show_unassigned; input normalized in configUpdate() |
| plugin install/update -> stored config | Migration writes enable_export flip; must not exceed one-time intent |
| datatable query -> DB | getRows() WHERE clause is the single enforcement point for entity + unassigned scoping |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | hook.php enable_export migration | high | mitigate | One-time marker (export_default_migrated) + is_upgrade guard: flip runs once, never overrides a post-1.2.0 admin disable (SC2 + prohibition) |
| T-02-02 | Information disclosure | getRows() WHERE (show_unassigned) | high | mitigate | Filter in SQL on `{$isc}.users_id`, shared by view + export; unassigned rows never leave DB when show_unassigned=0 |
| T-02-03 | Tampering | configUpdate() checkbox input | medium | mitigate | show_unassigned normalized to '0'/'1' string before persist, same as existing checkboxes |
| T-02-04 | Elevation of privilege | showConfigForm() write | low | accept | Config form already gated on core `config` right (READ to view, UPDATE to edit) — unchanged by this plan |
| T-02-SC | Tampering | dependency installs | low | accept | Phase adds NO third-party packages (npm/pip/cargo). No package legitimacy gate applicable |
</threat_model>

<verification>
- Static: all Task verify greps pass; po2mo.py regenerates both .mo files.
- Behavioral (deferred to Plan 03 checkpoint — PHP not runnable locally): on production after deploy, default catalog shows 42 rows; show_unassigned=1 shows 136; enable_export reads '1' after the 1.1.0->1.2.0 update.
</verification>

<success_criteria>
- enable_export default is '1'; one-time safe migration flips existing '0' to '1' on upgrade (SC2)
- show_unassigned key (default '0') hides unassigned SIMs in SQL; '1' restores full list (SC3/SC4)
- Version bumped to 1.2.0; new config label translated pl/en
</success_criteria>

<output>
Create `.planning/phases/02-eksport-csv-i-filtr-pustych-sim-w/02-01-SUMMARY.md` when done.

## Artifacts this phase produces (Plan 01)
- `src/Config.php`: `getDefaults()` key `show_unassigned` (default `'0'`); `enable_export` default changed `'0'`->`'1'`; `configUpdate()` normalizes `show_unassigned`; `showConfigForm()` new Yes/No row.
- `src/Simcard.php`: `getRows()` WHERE clause `"{$isc}.users_id" => ['>', 0]` when `show_unassigned` falsy.
- `hook.php`: one-time migration — stored config key `export_default_migrated` (marker) + conditional `enable_export` flip to `'1'`.
- `setup.php`: `PLUGIN_SIMVIEWER_VERSION` = `'1.2.0'`.
- `locales/pl_PL.po`, `locales/en_GB.po` (+ regenerated `.mo`): translatable string `"Show SIMs without assigned user"`.
</output>
